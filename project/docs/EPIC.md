# Epic — module-scale work

Use this when `orchestration.mode: recursive` in `harness.yaml`. For linear mode, you can ignore this file.

## Modules

_(Add unchecked lines like `- [ ] MODULE_01: Title — short description` when you split the app into sub-orchestrator modules.)_

## Global interface contracts

_(Optional `### MODULE_XX` subsections for cross-module contracts.)_
